[phases.setup]
# Nix package names (runtime libs) — provides libGL.so.1 etc.
nixLibs = [
  "mesa",          # libGL.so.1
  "glib",
  "xorg.libX11",
  "xorg.libXext",
  "xorg.libSM",
  "xorg.libICE",
  "xorg.libXrender",
  "freetype",
  "fontconfig",
  "zlib"
]
